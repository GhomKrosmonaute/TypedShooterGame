
export interface Keys {
    [key:string]: boolean
}

export enum MoveKeys {
    DOWN = 'ArrowDown',
    UP = 'ArrowUp',
    LEFT = 'ArrowLeft',
    RIGHT = 'ArrowRight'
}

export enum ShotKeys {
    DOWN = 's',
    UP = 'z',
    LEFT = 'q',
    RIGHT = 'd'
}
